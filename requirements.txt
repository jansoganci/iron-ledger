# Month Proof — Runtime Dependencies
# Python 3.11 — pin for Render/hosting via root .python-version (pandas 2.2.x wheels; avoid 3.14)
# CRITICAL: xlrd must be exactly 1.2.0 — later versions dropped .xls support
# CRITICAL: anthropic must be >= 0.40 for claude-opus-4-7 support
# CRITICAL: pydantic v2 only — use model_dump(), model_validate(), model_dump_json()

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
python-dotenv==1.0.1

pandas==2.2.2
openpyxl==3.1.5
xlrd==1.2.0
pandera==0.20.4
rapidfuzz==3.9.7

pydantic==2.8.2
pydantic-settings==2.4.0

anthropic==0.40.0
supabase==2.7.4
resend==2.4.0

slowapi==0.1.9
python-jose[cryptography]==3.3.0
jinja2==3.1.4

httpx==0.27.2
